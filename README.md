# 📝 Task Tracker — React + Express + JSON DB

A full-stack Task Tracker application with filtering by status, priority, light/dark themes, and a simple JSON file database (lowdb).  
Built with **Vite + React** on the frontend and **Node.js + Express** on the backend.

---

## 🚀 Features

- Add / Edit / Delete tasks
- Mark tasks as completed
- Filter by:
  - Status (All, Completed, Pending)
  - Priority (Low, Medium, High)
- Priority color badges
- Light / Dark theme support
- Persistent storage using **lowdb (JSON file DB)**
- Fully deployable on **Render** (Backend + Frontend)

---

## 📂 Project Structure
